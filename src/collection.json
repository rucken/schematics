{
  "$schema": "./node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "entity": {
      "description": "Generate model, service, grid, lookup input, modal for edit row in grid, modal for select items from grid with items",
      "factory": "./entity",
      "schema": "./entity/schema.json"
    },
    "new": {
      "description": "Generate empty application based on rucken template",
      "factory": "./new",
      "schema": "./new/schema.json"
    }
  }
}